# 安裝手冊（點餐趣）

本手冊說明如何在開發機或伺服器上建置 **點餐趣** 專案：**PostgreSQL、Redis、FastAPI 後端、Vue 管理後台**，以及選用的 Flutter 收銀端。適用於 Windows / macOS / Linux；路徑請依您的環境調整。

---

## 1. 前置需求

| 元件 | 建議版本 | 用途 |
|------|----------|------|
| Python | 3.11+ | 後端 API |
| Node.js | 20 LTS+ | 管理後台 (`apps/admin`) |
| Docker Desktop 或 Docker Engine | 近期版本 | PostgreSQL、Redis（建議） |
| Flutter SDK | 穩定版 | POS App（可選） |
| Git | - | 取得原始碼 |

---

## 2. 取得專案與基礎設施

### 2.1 啟動 PostgreSQL 與 Redis

在專案根目錄：

```bash
docker compose up -d postgres redis
```

預設連線（與 `docker-compose.yml` 一致）：

- PostgreSQL：`localhost:5432`，資料庫 `pos`，使用者 `pos`，密碼 `pos`
- Redis：`localhost:6379`

確認容器健康後再執行 migration。

### 2.2（可選）以 Docker 跑 API

根目錄已提供帶 `profiles: ["api"]` 的 API 服務。若要用容器跑後端：

```bash
docker compose --profile api up -d
```

開發時多數人會在本機直接跑 `uvicorn`（見下文），較利於除錯與熱重載。

---

## 3. 後端（FastAPI）

### 3.1 建立虛擬環境與安裝依賴

```bash
cd apps/api
python -m venv .venv
```

- **Windows（PowerShell）**：`.venv\Scripts\Activate.ps1`
- **macOS / Linux**：`source .venv/bin/activate`

```bash
pip install -r requirements.txt
```

### 3.2 環境變數

複製範例並編輯：

```bash
cp .env.example .env
```

**開發環境（`ENV=dev`）** 可容忍預設 `JWT_SECRET`；仍建議改為隨機字串。

**生產環境** 必須：

1. 設定 **`ENV=production`**（或 `prod` / `live`，依程式判定）。
2. 設定 **強隨機 `JWT_SECRET`**（不可用 `change-me`、`test-secret` 等預設值）。
3. 設定 **`SECRETS_ENCRYPTION_KEY`**（Fernet 金鑰，32-byte URL-safe base64），例如：

```bash
python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
```

其他常用變數已整理於 **`apps/api/.env.example`**（含中文區塊註解）；程式碼中的完整欄位定義見 `app/core/config.py`。摘要如下：

| 變數 | 說明 |
|------|------|
| `DATABASE_URL` | 非同步驅動字串，例如 `postgresql+asyncpg://pos:pos@localhost:5432/pos` |
| `REDIS_URL` | 限流後端等用途 |
| `SECRETS_ENCRYPTION_KEY` | 生產必填；租戶金鑰加密用 Fernet 金鑰 |
| `RATE_LIMIT_ENABLED` | 設 `false` 可減少測試時限流干擾 |
| `RATE_LIMIT_STORAGE_URI` | 空白時沿用 `REDIS_URL`；若 Redis 與 API 分機可另填 |
| `CORS_ORIGINS` | 允許的前端來源，逗號分隔 |
| `CAPTCHA_PROVIDER` / `CAPTCHA_SECRET` | `hcaptcha` 或 `turnstile`；申請表防機器人（未設定時開發常略過） |
| `SMTP_*`、`EMAIL_OTP_TTL_MIN` | 正式寄信與 OTP；未設定 SMTP 時開發環境可能僅從日誌看到驗證碼 |

平台級 **金流／發票** 變數在 `.env.example` 中預設為空（請勿將正式商編／Hash 寫入版本庫）；多租戶正式營運時應優先在後台 **租戶設定** 維護 **租戶專用** 商戶與金鑰（資料庫內加密儲存）。

### 3.3 資料庫遷移

仍在 `apps/api` 目錄、且虛擬環境已啟動：

```bash
alembic upgrade head
```

### 3.4 種子資料（示範租戶）

```bash
python -m app.scripts.seed
```

成功後終端會列印：

- 平台超管：`platform_super` / `platform-secret-CHANGE-ME`（**請立即於後台變更密碼**）
- 租戶代號：`demo`
- 店家代號：`S001`、終端：`T01`
- **終端 API Key**（僅此一次顯示，請保存供 POS 使用）

若資料庫已存在非 `__legacy__` 租戶，seed 會跳過以避免重複。

### 3.5 啟動 API 服務程式

```bash
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

- Swagger UI：<http://localhost:8000/docs>
- 就緒檢查：<http://localhost:8000/readyz>

---

## 4. 管理後台（Vue Admin）

```bash
cd apps/admin
npm install
```

可選：複製 `apps/admin/.env.example` 為 `.env`，設定 `VITE_CUSTOMER_BASE_URL`（列印 QR 點餐連結時使用）。

```bash
npm run dev
```

預設 <http://localhost:5173>。Vite 已將 **`/api` 與 `/uploads`** 代理至 `http://localhost:8000`，請先啟動後端。

正式建置：

```bash
npm run build
npm run preview   # 本地預覽靜態檔，實際部署請交由 Nginx 等反向代理
```

---

## 5. Flutter 收銀端（可選）

在 monorepo 根目錄：

```bash
dart pub global activate melos
melos bootstrap
cd apps/pos_app
flutter pub get
flutter run -d windows   # 或 android / macos 等
```

後端 Base URL 請見 `apps/pos_app` 內環境設定（如 `lib/config/env.dart`），須指向可連線的 API 位址。

---

## 6. 執行測試（後端）

在 `apps/api`：

```bash
pip install pytest pytest-asyncio httpx
python -m pytest tests/ -p no:warnings -p no:flask
```

若環境中安裝了 `pytest-flask`，建議加上 **`-p no:flask`**，避免與 FastAPI 測試客戶端衝突。測試預設會將 `RATE_LIMIT_ENABLED` 設為較寬鬆行為（見 `tests/conftest.py`）。

---

## 7. 部署檢查清單（摘要）

- [ ] `ENV` 設為 production
- [ ] `JWT_SECRET` 為強隨機且非黑名單預設值
- [ ] `SECRETS_ENCRYPTION_KEY` 已設定並備份於機密管理
- [ ] `DATABASE_URL`、`REDIS_URL` 指向正式叢集
- [ ] `CORS_ORIGINS` 僅列出正式前端網域
- [ ] CAPTCHA、SMTP 已設定（公開申請與 OTP）
- [ ] 反向代理終止 TLS，並限制內網管理介面曝露

更多日常使用步驟請見 **[操作手冊.md](./操作手冊.md)**。
