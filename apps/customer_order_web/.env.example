# API base used by axios. Dev 預設 /api，由 vite 轉到 http://localhost:8000（見 vite.config.ts）。
# 若後端不在本機 8000，請改成完整網址，例如：
#   VITE_API_BASE=https://pos.myvnc.com/api
# 並在後端 CORS_ORIGINS 加入顧客端網址（如 http://localhost:5174）。
VITE_API_BASE=/api
