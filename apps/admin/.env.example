# Base URL of the customer-facing ordering site. The admin uses this to
# build QR codes printed onto each table's QR sticker:
#   ${VITE_CUSTOMER_BASE_URL}/order?t=<public_token>
# Default for local dev assumes the customer SPA on Vite default port 5174.
VITE_CUSTOMER_BASE_URL=http://localhost:5174
