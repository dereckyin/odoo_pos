# 可選：後台 API 基底 URL。未設定時使用相對路徑 /api（由 Vite dev 轉發至 localhost:8000）。
# 若內嵌瀏覽器或本機路由（例如 *.taaze.tw）導致代理異常，可改為直接連後端：
# VITE_API_BASE_URL=http://localhost:8000
#
# 顧客掃碼點餐前台網址（不要尾隨 /）。後台列印 QR 用：
#   ${VITE_CUSTOMER_BASE_URL}/order?t=<public_token>
#
# 本機開發（顧客端 npm run dev，預設埠 5174）：http://localhost:5174
# 正式 build 見 .env.production（pos.myvnc.com）；或後台開在正式網域時會自動用「目前網域/customer」
# VITE_CUSTOMER_BASE_URL=http://localhost:5174
