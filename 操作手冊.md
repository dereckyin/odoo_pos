# 操作手冊（點餐趣）

本手冊說明 **點餐趣** 系統中 **平台營運者**、**店家管理員**、**收銀／POS** 與 **公開申請** 等常見操作流程。API 前綴預設為後端根路徑（本地多為 `http://localhost:8000`）；管理後台透過 Vite 將 `/api` 代理至後端，畫面上無需手動加前綴。

---

## 1. 角色與登入方式總覽

| 角色 | 後台登入 | 租戶代號欄位 | 說明 |
|------|----------|--------------|------|
| 平台超管 (`platform_super`) | `/auth/admin-login` | 可留白 | 審核開店申請、租戶列表；操作特定租戶資料時可透過 `X-Tenant-Id`（後端行為，進階） |
| 店家管理類 (`tenant_owner` / `tenant_admin` / `store_manager`) | `/auth/admin-login` | **建議填寫**，例如 `demo` | Vue 後台日常營運 |
| 收銀／廚房等 | `/auth/login`（POS） | **必填** | 需一併提供店面代號、終端代號、**終端 API Key**、帳密 |

首次以「一次性密碼」開通後，後台會要求變更密碼（依前端實作與 `must_change_password` 旗標）。

---

## 2. 點餐趣｜管理後台（Vue Admin）

### 2.1 登入

1. 開啟管理後台網址（開發預設 `http://localhost:5173`）。
2. 輸入 **租戶代號**（平台超管可留白）、**帳號**、**密碼**。
3. 若為首次登入且系統標記須改密碼，依對話框設定新密碼。

### 2.2 店家管理員常用選單

- **總覽**：儀表統計（僅限所屬租戶）。
- **產品管理**：分類、商品、CSV 匯入。
- **行銷方案**：促銷、優惠券。
- **會員管理**：會員與等級。
- **庫存管理**：水位、調撥。
- **門店帳號**：門店、桌位、使用者。
- **QR 點餐**：桌邊訂單監看。
- **訂單報表**：訂單查詢、銷售報表。
- **租戶設定**（具租戶管理權限者）：付款／發票商戶設定（金鑰不落前端明文回傳）、訂閱與用量、稽核紀錄；可下載 **租戶資料匯出**（JSON 快照，敏感欄位已排除）。
- **寄賣書籍**（模組已開啟時）：書籍列表、寄賣入庫、CSV 批次入庫、寄賣設定、分帳報表。

### 2.3 平台超管專區

當登入角色為 `platform_super` 時，側邊欄會出現 **平台管理**：

- **店家申請審核**：檢視 `email_verified` 狀態之申請、核准或拒絕；核准時選擇方案代號、管理員帳號、可選租戶代號等。
- **租戶管理**：檢視／編輯租戶狀態（如啟用、停權）。

公開 **店家申請開通** 表單路由通常為 `/signup`（無需登入）。

---

## 3. 新店家開通流程（申請審核制）

### 3.1 申請人（公開頁）

1. 開啟後台網站的 **申請開通** 連結（登入頁上之「申請開通新店家」或 `/signup`）。
2. 填寫公司資料、聯絡信箱等並送出。
3. 至信箱取得 **6 碼 OTP**，於第二步完成驗證。
4. 狀態轉為待審核後，由平台超管於後台核准。

### 3.2 平台審核通過後

系統會建立租戶、預設店面、Owner 帳號，並寄送 **租戶代號** 與 **一次性密碼**（實際寄信依 SMTP 設定）。Owner 首次登入應立即變更密碼。

---

## 4. 點餐趣｜Flutter 收銀端

### 4.1 終端註冊（需管理員）

終端機 **API Key** 須由具 **店家管理權限** 的帳號產生，不可匿名註冊：

1. 在 POS 開啟「終端註冊」畫面。
2. 輸入 **租戶代號**、**管理員帳密**，必要時輸入店面代號與新終端代號。
3. App 會先以管理員身分登入，再呼叫後端註冊終端並取得 **API Key**；請依指示儲存。

### 4.2 日常登入（收銀）

1. 輸入 **租戶代號**、**店面代號**、**終端代號**。
2. 使用已向管理員取得的 **終端 API Key**（通常由本機安全儲存帶入）。
3. 輸入 **使用者帳號**、**密碼**（例如 `cashier` / `cashier123` 僅供示範）。

若登入失敗，請確認：租戶狀態為啟用、終端仍存在、API Key 未被管理員旋轉後未更新。

### 4.3 印表機與列印（收據／廚房製作單）

收銀 App 透過 **ESC/POS 熱感印表機**、以 **TCP 9100** 列印兩類單據：

| 單據 | 何時列印 | 設定位置 |
|------|----------|----------|
| **收據** | 結帳成功後自動列印（列印失敗不影響結帳） | 設定 → 印表機 → **收銀（前場）** |
| **廚房製作單** | 桌邊 QR 訂單按 **接單出餐並結帳**（或 KDS 接單）時先列印，成功後才更新狀態 | 設定 → 印表機 → **廚房（後場）** |

#### 硬體與網路

1. 使用支援 **ESC/POS** 的 58mm 或 80mm 網路熱感機（RJ-45 或 Wi‑Fi）。
2. 為印表機設定 **固定 IP**（例如櫃台 `192.168.1.100`、廚房 `192.168.1.110`），埠號預設 **9100**。
3. 從 POS 平板 `ping` 印表機 IP，確認同一區網可連線。

#### 初次設定

1. 登入 POS → **設定** → **印表機**。
2. 在 **收銀（前場）** 分頁：開啟「啟用收銀印表機」、填 IP／埠、選紙寬 → **儲存**。
3. 在 **廚房（後場）** 分頁：若門店有 QR 點餐，建議另開一台廚房機並啟用；未啟用時接單仍成功，僅不印製作單。
4. 按 **測試列印**（會先儲存設定再送出一張標示「測試列印」的樣張）確認有出紙。

#### 日常操作

- **收據**：一般結帳流程完成即自動列印；內容含品項、金額、付款方式。
- **廚房單**：收銀在 **桌邊訂單** 按 **接單出餐並結帳**（或廚房帳號於 **KDS** 分步接單）；製作單含桌號、品項與數量，**不含價格**。
- 廚房印表機若已啟用但連線失敗，**接單會失敗**且訂單維持已送出，修正網路後可重試。

### 4.4 桌邊訂單（QR 點餐 · 收銀一人包辦）

客人掃桌位 QR 下單後，品項**不會自動出現在收銀主畫面**；收銀需至 **桌邊訂單** 帶入購物車後，再走與一般點餐相同的結帳流程。

#### 日常操作

1. 主畫面右上角 **桌邊訂單** 圖示若有數字 Badge，表示有待接單；購物車為空時右側也會提示筆數。
2. 進入 **桌邊訂單** → 在 **待接單** 區按 **接單出餐並結帳**：
   - 列印廚房製作單（若已啟用廚房印表機）
   - 品項帶入購物車
   - 自動進入結帳頁
3. 完成付款、會員／促銷、電子發票等，與一般櫃台結帳相同。

#### 備援（異常或舊單）

- **待結帳** 區（烹調中或已出餐但未結帳）可按 **帶入結帳**，同樣帶入購物車並進入結帳頁。

#### 有獨立廚房時

- 廚房帳號登入 **廚房看板（KDS）** 可維持分步接單（待接受 → 烹調中 → 待結帳）；收銀僅在 **待結帳** 區 **帶入結帳** 即可。

#### 疑難排解

| 狀況 | 建議 |
|------|------|
| 接單出餐並結帳失敗 | 多為廚房印表機連線問題；修正後重試，或至 **待結帳** 按 **帶入結帳** |
| 購物車已有商品 | 匯入前會詢問是否清空；確認後再帶入桌邊訂單 |


| 狀況 | 建議 |
|------|------|
| 測試列印失敗 | 檢查 IP、9100 埠、防火牆、印表機電源與紙張 |
| 結帳成功但無收據 | 確認收銀印表機已「啟用」並已儲存 |
| 接單失敗 | 若廚房印表機已啟用，多為網路問題；可暫時關閉啟用讓接單先通過 |
| 紙張裁切或字體異常 | 在設定頁將紙寬改為與實機相符（58 或 80mm） |

管理後台 **桌位 QR** 的 A4 列印屬瀏覽器 `window.print()`，與上述熱感機無關，見 **門店帳號 → 桌位管理 → 列印選取**。

### 4.5 寄賣書籍（進貨 vs 結帳）

寄賣書籍與一般餐飲商品不同：**TAAZE 書目查詢與建檔發生在「入庫」**，**結帳掃碼只查本地已入庫書籍**。

#### 前置（平台／店家）

1. 平台超管於 **租戶管理** 開啟「寄賣書籍」模組。
2. 店家於 **寄賣書籍 → 寄賣設定** 設定分帳比例、開放門店、POS 折扣快捷鈕。

#### 進貨入庫（建立主檔 + 加庫存）

| 方式 | 操作位置 | 說明 |
|------|----------|------|
| 單本掃碼 | 後台 **寄賣入庫** | 輸入／掃 11 碼 TAAZE 商品編號 → 查書預覽 → 選門店、數量 → 確認入庫 |
| CSV 批次 | 後台 **CSV 批次入庫** | 上傳 `barcode,qty,store_code`（或 `store_id`）；每列自動查 TAAZE 並入庫 |
| 現場收書 | POS（**store_manager 以上**） | 收銀畫面 **寄賣入庫** 圖示 → 掃碼查書 → 確認入庫 → 建議執行同步 |

入庫後請在 POS 按 **同步**（或等待自動同步），收銀台才能掃到該書。

#### 銷售結帳（POS）

1. 收銀主畫面 **掃碼** 或 **寄賣書籍查詢**（僅顯示已同步、已入庫的書）。
2. 系統**不會**在結帳時呼叫 TAAZE；若掃到未入庫條碼，會提示「請先至寄賣入庫」。
3. 購物車可點寄賣折扣 Chip；一般促銷不套用寄賣書。
4. 結帳後依實際成交價寫入分帳快照；報表見 **寄賣書籍 → 分帳報表**。

#### 與「產品 CSV 匯入」的差異

- **產品管理 → CSV 匯入**：一般餐飲商品，不含 TAAZE、不含 `book_details`。
- **寄賣書籍 → CSV 批次入庫**：專用格式，會查 TAAZE 並寫入寄賣書主檔與庫存。

---

## 5. 金流與發票（概念）

- **租戶設定** 中可維護 **每租戶** 的付款閘道（如 ECPay、NewebPay、LINE Pay）與電子發票閘道；敏感金鑰於伺服端加密儲存，列表 API 不會回傳明文金鑰。
- 若租戶未設定某驅動程式，後端可能回退使用 **平台 `.env` 中的預設商戶參數**（僅建議開發／共用沙箱使用）。
- 實際請款、退刷、開票作廢等，仍須依各閘道正式文件與合約開通。

---

## 6. 訂閱與用量（概念）

- 租戶可綁定 **訂閱方案**（方案內含店面數、終端數、商品數、每月訂單上限等）。
- 超過方案限制時，部分建立資源的操作會回傳 **402** 類錯誤，需升級方案或調整方案參數（依營運政策）。
- **訂單上傳** 會佔用每月用量計數；重複相同訂單 ID 的 idempotent 行為依後端實作不會重複計費。

---

## 7. 安全與稽核

- **登出** 會撤銷所提供的 refresh token；**變更密碼** 會撤銷該使用者所有尚未撤銷的 refresh token。
- **稽核紀錄**可在「租戶設定」分頁檢視（限該租戶範圍）；平台超管若需跨租戶稽核需配合後端設計使用。
- 圖片上傳等靜態資源，後端會依租戶分目錄儲存，便於日後退場或清理。

---

## 8. 常見問題（FAQ）

**Q：後台登入一直 401？**  
檢查租戶代號是否與帳號所屬租戶一致；平台超管可嘗試租戶代號留白。

**Q：POS 顯示終端認證失敗？**  
確認 `terminal_api_key` 與後端該終端最新金鑰一致；若管理員曾「重設終端／註冊新金鑰」，須在 POS 重新設定或重新註冊。

**Q：公開申請收不到信？**  
檢查後端是否已設定 `SMTP_*`（範例鍵名見 **`apps/api/.env.example`**）；開發環境未設定 SMTP 時，OTP 可能僅出現在 API 行程日誌（見 `app/core/notify.py`）。

**Q：生產環境無法啟動 API？**  
檢查 `ENV` 是否為 production 且已設定合法 `JWT_SECRET` 與 `SECRETS_ENCRYPTION_KEY`。

---

如需建置步驟，請參閱 **[安裝手冊.md](./安裝手冊.md)**。
