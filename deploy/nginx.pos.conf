# 網頁收銀 SPA（pos_web build，base=/pos/）
# 對外由主機 Caddy handle_path /pos 反代至此容器。

server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    charset utf-8;
    client_max_body_size 10m;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
